# Jest + Cucumber pour les tests, plutôt que les défauts Angular

Angular CLI configure Karma/Jasmine par défaut ; on utilise Jest pour les tests unitaires (front et back) et Cucumber pour les tests end-to-end. Décision explicite pour éviter que quelqu'un réintroduise Karma/Jasmine en pensant suivre le défaut Angular CLI.
